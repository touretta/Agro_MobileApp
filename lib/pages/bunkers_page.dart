import "package:flutter/material.dart";

class BunkersPage extends StatelessWidget {
  const BunkersPage ({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("tyt nada sdelat' karoch"),)
    );
  }
}